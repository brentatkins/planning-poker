import FirebaseContext from "./context";
import Firebase from "./firebase";
import useFirebaseCollection from "./useFirebaseCollection";

export default Firebase;

export { FirebaseContext, useFirebaseCollection };
